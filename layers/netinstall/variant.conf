# Values substituted into the @PLACEHOLDER@s of the *.in templates in
# layers/base (profiledef.sh, boot menus, /etc/dev-rel).
# Read by tools/merge-profile.sh; this file is NOT copied into the ISO.

VARIANT_ID="netinstall"             # lowercase id, shown in the BIOS menu title
VARIANT_NAME="NetInstall"           # display name, e.g. "BlackArch Linux NetInstall ISO"
ISO_NAME="blackarch-linux-netinst"   # profiledef.sh iso_name
VARIANT_PURPOSE="perform system maintenance"   # "...install BlackArch Linux or <this>."

# Extra profiledef.sh file_permissions entries, one per line (may be empty)
FILE_PERMISSIONS='
  ["/usr/local/bin/wifi-help"]="0:0:755"
'
